spring:
  application:
    name: ${APP_NAME:gateway}
  config:
    import:
      - optional:nacos:common.yaml?server-addr=${NACOS_SERVER_ADDR:172.26.76.190:8848}&group=DEFAULT_GROUP
      - optional:nacos:gateway.yaml?server-addr=${NACOS_SERVER_ADDR:172.26.76.190:8848}&group=DEFAULT_GROUP
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR:172.26.76.190:8848}
        group: DEFAULT_GROUP
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:172.26.76.190:8848}
        namespace: ${NACOS_NAMESPACE:}

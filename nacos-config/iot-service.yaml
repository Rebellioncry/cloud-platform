spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://${MYSQL_HOST:172.26.76.190}:${MYSQL_PORT:3306}/cloud_platform?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: ${MYSQL_USERNAME:root}
      password: ${MYSQL_PASSWORD:root123}
      maximum-pool-size: 10
      minimum-idle: 2

tdengine:
  datasource:
    hikari:
      jdbc-url: jdbc:TAOS-WS://${TDENGINE_HOST:172.26.76.190}:${TDENGINE_PORT:6041}/iot_db?user=root&password=taosdata&varcharAsString=true
      driver-class-name: com.taosdata.jdbc.ws.WebSocketDriver
      username: root
      password: taosdata
      maximum-pool-size: 5
      minimum-idle: 1

server:
  port: ${SERVER_PORT:8085}

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: org.lyz.iot.entity

iot:
  platform:
    username: iot-platform
    password: iot-platform-key
